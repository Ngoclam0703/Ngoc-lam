#include <stdio.h>
	int main (){
		int a,b;
		printf("nhap vao 2 so can kiem tra ");
		scanf("%d %d",&a,&b);
		if (a>b){
	    printf("so nho hon la %d\n",b);
		}
	    if (a<b){
	    printf("so lon hon la %d\n",b);	
		}
		return 0;
	}
	
